<Window x:Class="Mk20Box.Ui.NamePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="420"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#0F151B"
        Foreground="#E6EAEE"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText"
                   Margin="0,0,0,8"
                   Opacity="0.75"
                   TextWrapping="Wrap" />

        <TextBox x:Name="NameBox" Grid.Row="1"
                 Height="28"
                 VerticalContentAlignment="Center"
                 Background="#1B242D" Foreground="#E6EAEE" BorderBrush="#2A3742" />

        <StackPanel Grid.Row="2" Margin="0,12,0,0"
                    HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Width="90" Height="28" Margin="0,0,6,0"
                    Click="Ok_Click" Content="OK" IsDefault="True" />
            <Button Width="90" Height="28" Content="Cancel" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
