<Window x:Class="Mk20Box.Ui.PropertyPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose a SimHub value"
        Width="620" Height="520"
        Background="#0F151B"
        Foreground="#E6EAEE"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Margin="0,0,0,8" Opacity="0.75" TextWrapping="Wrap"
                   Text="Common values are listed first. Anything the running game exposes is searchable too - type to filter." />

        <TextBox x:Name="SearchBox" Grid.Row="1" Height="28" Margin="0,0,0,8"
                 VerticalContentAlignment="Center"
                 Background="#1B242D" Foreground="#E6EAEE" BorderBrush="#2A3742"
                 TextChanged="Search_TextChanged" />

        <ListBox x:Name="ResultList" Grid.Row="2"
                 Background="#141B22" Foreground="#E6EAEE" BorderBrush="#2A3742"
                 MouseDoubleClick="Result_DoubleClick" />

        <StackPanel Grid.Row="3" Margin="0,10,0,0"
                    HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Width="90" Height="28" Margin="0,0,6,0"
                    Click="Ok_Click" Content="Choose" IsDefault="True" />
            <Button Width="90" Height="28" Content="Cancel" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
