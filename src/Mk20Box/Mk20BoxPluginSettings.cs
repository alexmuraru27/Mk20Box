namespace Mk20Box
{
    /// <summary>
    /// Persisted plugin settings. Must remain JSON-serializable (JSON.NET).
    /// Add fields here as the plugin grows (COM port, canvas options, etc.).
    /// </summary>
    public class Mk20BoxPluginSettings
    {
    }
}
